# Handoff

> Create or update a handoff only at a real execution boundary with concrete
> resumption risk for a later agent or session. Neither boundary nor risk alone
> is enough. Keep entries compact and task-first; presentation is configurable.
> Remove or replace when the risk is consumed—never retain as history.

<!--
Use one current entry per task or workstream (`## T-001 — Title` when a task ID
exists). Include only the additional transient state needed to mitigate a
concrete risk—recommended: checkpoint, risk, and next safe action when those
are not already obvious from Git or todo.md.

Do not organize by agent, harness, session, or date. Do not append progress
narration or accumulate multiple snapshots of the same work. Do not duplicate
todo.md metadata to satisfy a format. Do not create an entry merely because a
session closed, a task moved to Ready to Land or Blocked, or a live collision
was coordinated in todo.md.
-->
